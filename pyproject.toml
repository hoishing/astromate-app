[project]
name = "astrobro"
version = "1.0.0"
description = "your pocket astrologer 💫"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "natal",
    "st-screenwidth-detector",
    "streamlit",
    "streamlit-shortcuts",
    "google-genai",
]

[tool.ruff]
isort.no-sections = true
line-length = 100
extend-ignore = ["E203", "E402", "E501", "E712", "F401", "F811"]

[tool.uv.sources]
natal = { path = "../natal" }

[dependency-groups]
dev = ["ipykernel", "watchdog"]
